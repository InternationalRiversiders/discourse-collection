# discourse-collection

[English](./README.en.md) | 简体中文

---

Discourse 插件：公开的「淘专辑」。

登录用户可以创建专辑，把论坛里任意主题收录进来，精选其中的优质回复，并与他人共同维护。专辑是人工整理的公开清单，可供浏览、订阅与被收录话题的反向查找。

## 功能

- **收录**：创建专辑并收录任意主题，可为每条收录写备注；主题页可反查它被哪些专辑收录。

- **精选回复**：把主题内的优质回复精选进专辑；专辑阅读页按行内联展示，超出上限时可展开全部。移除收录会连带删掉该话题的精选回复且不可恢复，故移除前会告知条数并可先查看。

- **订阅与通知**：任意登录用户可订阅专辑；专辑收录新主题时，订阅者收到 Discourse 原生通知。同一专辑在通知列表里**始终只占一条**，每次收录把它重新标为未读并点亮红点，不逐条堆积。

- **共同维护**：owner 邀请他人成为共同维护者，对方接受后生效；owner 可移除维护者，共同维护者也可靠自己离队。

- **转让所有权**：owner 可发起换主邀请；staff 也可对任意专辑（含因原主注销而「无主」的专辑）发起。一律需受邀人接受后生效。

- **链接卡片**：帖子正文里的专辑链接独占一行时渲染为专辑卡片（名称、描述、计数、所有者、时间），位于句子中间时渲染为「专辑名 - 淘专辑」的链接。默认关闭。

- **游客浏览**：默认关闭，开启后未登录用户可浏览专辑。

- **个人主页**：用户个人主页的活动页有「淘专辑」标签页，列出该用户创建或维护的全部专辑，按最近收录排序。

- **管理**：管理员（及经设置授权的版主）可修正任意专辑的名称 / 描述、改写收录备注；相关改动进 Discourse 原生的「员工操作日志」。

可见性：专辑内的主题 / 帖子列表一律在**服务端按访问者权限过滤**，无权访问的条目直接剔除（与论坛列表口径一致，但包含通知级别为免打扰的话题）——专辑不会成为绕过论坛权限的通道。

## 使用

1. 左侧边栏「淘专辑」进入列表页，页签为「全部 / 我的 / 订阅 / 邀请」。

2. 在「我的」页创建专辑（名称 + 描述）。

3. 在任意主题的帖子操作栏「更多」里选「收录到专辑」，挑一个自己的（或有管理权的）专辑；同一入口也能精选单条回复。

4. 专辑详情页可订阅、查看收录的话题流、管理团队与邀请。

5. 被邀请时，通知与「邀请」页签都会给出提示，在那里接受或拒绝。

## 安装

按 Discourse 插件的常规方式安装即可，无额外依赖（数据库迁移会建 6 张表）。安装后确认 `collection_enabled` 与各条准入群组设置符合预期。

开启「链接卡片」后，存量帖子需**重新烘焙**（rebake）才会出现卡片。

## 站点设置

设置项都在站点设置的「插件」分类下，各有说明与默认值；以下是常用的几项：

| 设置 | 默认 | 说明 |
| --- | --- | --- |
| Collection enabled | 开 | 插件总开关 |
| Collection allow anonymous | 关 | 未登录用户可浏览专辑 |
| Collection moderators can manage collections | 关 | 版主可管理他人专辑（改名称、描述或发起换主）；管理员恒可 |
| Collection create allowed groups | `trust_level_1` | 可创建专辑、可被邀为新 owner 的群组；留空 = 关闭 |
| Collection teamworker allowed groups | `trust_level_1` | 可被邀为共同维护者的群组；留空 = 关闭 |
| Collection create disallowed groups | 空 | 不允许创建专辑、担任新 owner 的群组；留空 = 不拒绝任何人 |
| Collection teamworker disallowed groups | 空 | 不允许成为共同维护者的群组；留空 = 不拒绝任何人 |

准入按**群组成员身份**判定，且只在操作发生的那一刻判定；**不允许组优先于允许组**（同时命中即被拒）。豁免上限的角色设置只豁免数量上限，不豁免准入。

## 开发

- 仓库的开发约定与 schema 硬规则见 [CLAUDE.md](CLAUDE.md)。

- HTTP API 设计见 [docs/index.md](docs/index.md)；建表迁移是 schema 的唯一事实源。

[专辑头像与背景：上传、权限与附件生命周期](docs/collection-appearance.md)。

默认排序与自适应卡片布局见 [说明](docs/collection-reading-defaults.md)。
